package backend;

public class SessionProtection {
	private static Integer currentCollageId = 0;
	private static Integer defaultToken = 0;
	private static Integer validToken = 1; //change from DESIGN
	
	public Boolean checkNewSession(Integer userToken) {
		
		return false;
	}
}
